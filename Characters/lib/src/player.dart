library characters.player;

import 'character.dart';

class Player extends Character {

  Player(String chrType, String name, int helth) : super(chrType, name, helth);

  void update(){
    super.update();
  }

}
